<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.summ.mapper.AdminMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		admin_id AS adminId, admin_name AS adminName, admin_phone AS adminPhone, admin_type AS adminType, createid, modifyid, createtime, modifytime, isDel, admin_belong AS adminBelong, shop_id AS shopId, train_id AS trainId
	</sql>

	<select id="getCount" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM admin;
	</select>

</mapper>